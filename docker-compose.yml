# docker-compose.yml
services:
  api:
    build: .
    container_name: smarteye-hub
    restart: unless-stopped
    privileged: true
    pid: host
    env_file:
      - ./secrets/openai.env
    environment:
      - PORT=80
      - MEDIAMTX_API_URL=http://mediamtx:9997
      - MEDIAMTX_API_USER=${MEDIAMTX_API_USER:-apiuser}
      - MEDIAMTX_API_PASS=${MEDIAMTX_API_PASS:-apipass}
      - PLAYBACK_CLIP_MODE=copy-first
      - PLAYBACK_CLIP_PRESET=ultrafast
      - PLAYBACK_CLIP_THREADS=1
      - PLAYBACK_CLIP_CACHE_LIMIT=8
      - RECORDING_SEGMENT_SECONDS=8
      - RECORDER_POLL_SECONDS=5
      - INDEXER_POLL_SECONDS=3
      - NVME_BASE=/mnt/nvme/smarteye
    ports:
      - "${PORT:-80}:80"
      - "443:443"
    volumes:
      - ./data:/app/data
      - ./secrets:/app/secrets
      # rslave: host mount changes under /mnt/nvme propagate into the container,
      # so `mkfs.ext4 + mount` from the API is visible here immediately.
      - type: bind
        source: /mnt/nvme
        target: /mnt/nvme
        bind:
          propagation: rslave
          create_host_path: true
      - /sys/firmware/devicetree/base:/sys/firmware/devicetree/base:ro
      # PAM auth: mount host user database (read-only)
      - /etc/passwd:/etc/passwd:ro
      - /etc/shadow:/etc/shadow:ro
      - /etc/group:/etc/group:ro
      - /etc/pam.d:/etc/pam.d:ro
      - /etc/nsswitch.conf:/etc/nsswitch.conf:ro
      - /lib/aarch64-linux-gnu/security:/lib/aarch64-linux-gnu/security:ro
    extra_hosts:
      - "mediamtx:host-gateway"
    depends_on:
      - mediamtx

  mediamtx:
    image: bluenviron/mediamtx:latest
    container_name: mediamtx
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./mediamtx.yml:/mediamtx.yml:ro
    environment:
      - MTX_CONF=/mediamtx.yml
